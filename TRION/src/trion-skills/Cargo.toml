[package]
name = "trion-skills"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Bounded supervised-autonomy task plans and skill execution for Trion"

[dependencies]
eyre = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
trion-runtime = { path = "../trion-runtime" }
